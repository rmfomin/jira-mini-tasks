# Smart Tasks — Tampermonkey Script for JIRA Dashboard

## 📘 Описание проекта

**Smart Tasks** — пользовательский Tampermonkey‑скрипт, который внедряет интерактивный To‑Do‑лист прямо в виджет дашборда JIRA, поддерживает привязку задач к Jira‑issues и демонстрационные вызовы Jira REST API.

Скрипт:
- Находит контейнер гаджета `gadget-20269-chrome` и безопасно инициализируется после его появления.
- Заменяет его контент на собственный UI.
- Позволяет добавлять, редактировать, удалять, менять порядок и отмечать выполнение задач.
- Поддерживает необязательную привязку задачи к Jira‑ссылке (URL или Markdown `[Текст](https://...)`) с однострочным превью.
- Содержит панель с примерными вызовами Jira REST API (по кнопкам) — не влияет на локальную логику To‑Do.
- Хранит задачи в Tampermonkey‑хранилище (`GM_getValue` / `GM_setValue`), привязанных к текущему пользователю.

---

## ⚙️ Архитектура и структура кода

Скрипт реализован как **самодостаточный IIFE-модуль**.

Основные разделы кода:

1. **Константы**
   - Все идентификаторы, селекторы и интервалы вынесены в начало.
   - Комментарии для констант не добавляются.

2. **Утилиты**
   - `pollForGadget()` — безопасное ожидание загрузки контейнера.
   - `empty(el)` — очистка DOM‑элемента.
   - `el(tag, attrs, children)` — вспомогательный конструктор DOM‑узлов (поддерживает `text`/`html`/`className`).
   - `autosizeTextarea(ta)` — автоподгон высоты `textarea` до 6 строк без вертикального скролла.
   - `setupGlobalMenuCloser()` — глобально закрывает выпадающие меню по клику вне элементов.
   - `jiraRequest({ method, url, body, message })` — обертка над `fetch` с `credentials: 'include'`, JSON‑парсингом и логированием.

3. **Хранилище**
   - `loadTasks()` и `saveTasks()` — интерфейс для Tampermonkey-API.
   - Формат хранения:
     ```json
     [
       { "id": 1731252321, "text": "Задача", "done": false, "createdAt": 1731252321 }
     ]
     ```

4. **UI-логика**
   - `renderUI(root, tasks)` — основная отрисовка интерфейса, форма добавления, подсказки и демо‑панель API.
   - `renderItem(task)` — рендер одной задачи, бургер‑меню удаления, чекбокс, превью Jira‑ссылки.
   - `startEditMode(row, task)` — inline‑редактирование по клику на текст, кнопки Сохранить/Отмена, добавление/отключение Jira‑ссылки.
   - `rerenderList(listEl, tasks)` — перерисовка списка при изменениях.

   Дополнительно:
   - Drag‑and‑drop: ручка слева; изменение порядка; запрет DnD при редактировании; фиксация порядка в хранилище.

5. **Точка входа**
   - `bootstrap(gadgetEl)` — инициализация приложения.
   - `main()` — запуск опроса контейнера и инициализации UI.

---

## Правила кодирования и форматирования

### Общие принципы
- Код должен быть **читаемым человеком**, без сжатия и минификации.
- Соблюдать **единый стиль** во всех блоках.
- Использовать **строгий режим** (`'use strict'`).

### Комментарии
- Комментарии **только над блоком кода** (не сбоку).
- Каждый логический блок или функция имеет **краткое описание сверху**:
- названия должны быть самодокументирующими.

### Блоки `if`

* Всегда использовать фигурные скобки:

  ```js
  if (condition) {
    action();
  }
  ```

### Функции

* Именуются глаголом в **camelCase**: `loadTasks`, `saveTasks`, `renderUI`.
* Все функции содержат краткое описание и следуют принципу **Single Responsibility**.
* Вложенные функции не создаются без необходимости (предпочтение отдельным определённым функциям).

### DOM‑манипуляции

- Все элементы создаются через утилиту `el()` для унификации.
- В `el()` атрибут `html` использовать только при явной необходимости форматирования.

### Логика интерфейса

* Добавление задачи:

  * При каждом добавлении выполняется повторный вызов `loadTasks()` для получения актуальных данных.
  * Новая задача добавляется в конец списка.

* Редактирование задачи:

  * Вход по клику на текст задачи.
  * Inline‑режим (`textarea` + кнопки "Сохранить"/"Отмена"); действия выводятся под строкой.
  * Возможность подключить/отключить Jira‑ссылку; поддержка URL и Markdown.
  * Обновление данных в Tampermonkey‑хранилище и моментальная перерисовка.

* Удаление задачи:

  * Через бургер‑меню; меню закрывается глобальным обработчиком кликов вне меню.

* Перетаскивание:

  * Кастомная реализация DnD без использования HTML5 Drag and Drop API.
  * Перестановка только за «ручку» слева; в режиме редактирования DnD отключен.
  * При захвате элемент остается на месте с opacity 0.3, а рядом с курсором появляется полупрозрачный клон.
  * Между задачами подсвечивается синяя линия-индикатор места вставки.
  * При отпускании элемент перемещается в указанное место или возвращается на исходную позицию при невалидном дропе.
  * Порядок сохраняется по текущему DOM через `persistOrderFromDom`.

* Подсказки:

  * Под формой добавления и при редактировании выводятся подсказки по горячим клавишам.

### Горячие клавиши

- Добавление: `Enter`.
- Сохранение редактирования: `Enter`.
- Новая строка (добавление и редактирование): `Shift + Enter`.
- Отмена редактирования: `Esc`.

### Верстка

* Стиль встроенный (`style.*`), без внешних CSS-зависимостей.
* Визуальные принципы: минимализм, читаемость, единая цветовая палитра (`#4a9ae9`, `#f3f3f3`, `#4caf50`).

---

## 🧠 Логика и поведение

1. **Инициализация**

  * Периодически проверяет наличие гаджета `#gadget-20269-chrome`.
  * После появления — выполняет `bootstrap()`.

2. **Отрисовка**

  * Заменяет контент гаджета собственным wrapper‑контейнером; добавляет форму, список и демо‑панель API.

3. **Работа списка**

  * Добавление задач.
  * Чекбокс выполнения.
  * Inline-редактирование текста.
  * Автосохранение после каждого действия.

4. **Хранение**

  * Все данные сохраняются в `GM_setValue(STORAGE_KEY, JSON.stringify(tasks))`.
  * При загрузке — `GM_getValue(STORAGE_KEY, '[]')` с безопасным `JSON.parse` и fallback на `[]`.
  * Порядок задач соответствует порядку элементов в DOM и фиксируется при DnD.
  * Формат элемента поддерживает дополнительные поля `jiraUrl`/`jiraText` (опционально):
    ```json
    {
      "id": 1731252321,
      "text": "Задача",
      "done": false,
      "createdAt": 1731252321,
      "jiraUrl": "https://jira.theteamsoft.com/browse/KEY-123",
      "jiraText": "[KEY-123] Summary"
    }
    ```
  * Демо‑панель API выполняет запросы к Jira для примеров/отладки и не влияет на локальное состояние задач.

---

## 🧩 Правила развития проекта

1. **Сохранить архитектуру**

  * Не смешивать логику рендера, данных и состояния в одном блоке.
  * Все изменения должны быть атомарными и комментированы в едином стиле

2. **Работа с Tampermonkey API**

  * Хранение данных только через `GM_getValue` / `GM_setValue`.
  * Не использовать `localStorage`, чтобы данные не терялись между вкладками.

3. **Тестирование**

  * Проверить добавление, редактирование, выполнение, перезагрузку страницы.

4. **Будущее развитие**

  * Автоподтягивание названия по ключу тикета (REST API) с авто‑формированием Markdown‑ссылки.
  * Фильтрация задач (все / активные / выполненные).
  * Группировка/сворачивание по issue‑key.
  * Ослабление жёсткой привязки к `numberId` через дополнительный поиск контейнера.

---

## 🔌 API

- Обертка `jiraRequest`:
  - Добавляет `credentials: 'include'`, логирует ответ, безопасно парсит JSON; возвращает `null` при ошибках.
  - Использовать для внутренних запросов к Jira‑REST на том же домене.

- Демо‑панель API в UI:
  - «API: Мои задачи» — POST `/rest/api/2/search` с JQL `assignee = currentUser()`.
  - «API: ServerInfo» — GET `/rest/api/2/serverInfo`.
  - Кнопки предназначены для примеров/отладки и не меняют локальное состояние To‑Do.

Рекомендации:
- Не смешивать сетевую логику с базовым CRUD‑потоком To‑Do.
- Новые вызовы оформлять через `jiraRequest` и выносить в отдельные обработчики/кнопки.

---

## 🔖 Версионность

|   Версия  | Изменения                                                         |
| :-------: | :---------------------------------------------------------------- |
|  **0.1**  | Базовый каркас, smoke-тест, отрисовка контейнера                  |
|  **0.2**  | Реализован todo-лист с Tampermonkey-хранилищем                    |
| **0.2.1** | Исправлено добавление задач (новая задача не заменяет предыдущие) |
| **0.2.2** | Добавлена кнопка удаления для каждой задачи                       |
| **0.2.3** | Кнопка редактирования заменена на эмодзи; удаление перенесено в бургер‑меню |
| **0.2.4** | Исправлено скрытие кнопок Сохранить/Отмена при редактировании             |
| **0.2.5** | При редактировании кнопки Сохранить и Удалить перенесены под строку       |
| **0.3.0** | Перетаскивание задач за ручку; inline‑редактирование по клику; бургер‑меню удаления + глобальное закрытие; автосайзинг `textarea`; Jira‑ссылка под задачей (URL/Markdown) с однострочным превью; горячие клавиши; демо‑панель Jira API |
|| **0.3.1** | Переработка DnD: кастомная реализация с клоном элемента, линией-индикатором места вставки и возвратом на исходную позицию при невалидном дропе |
|| **0.3.2** | Сохранение переносов строк в тексте задач (добавлен CSS white-space: pre-wrap) |
|| **0.3.3** | Изменены горячие клавиши: Enter — добавить/сохранить, Shift+Enter — новая строка, Esc — отменить |

---

## 🧭 Основные принципы для ИИ-помощников

1. Поддерживать существующий **стиль форматирования и комментариев**.
2. При добавлении новых функций — использовать формат:
3. Новые константы — без комментариев, только логичные имена.
4. Все правки должны быть **детерминированными и описаны в changelog-стиле**.
5. При сомнении — **сохранять читаемость и безопасность выше краткости**.
6. Любое изменение логики UI должно сохранять обратную совместимость со структурой данных в `GM_getValue`.
7. Дополнять данный файл в случае изменения логики, функционала и прочего.
8. Отвечать только на русском языке

Дополнительно:
- Сохранять текущую архитектуру разделов: утилиты → хранилище → UI → DnD → точка входа.
- Новые сетевые вызовы — только через `jiraRequest` с теми же принципами логирования.
- Обновлять селекторы/`numberId` аккуратно: контейнер — `#gadget-<numberId>-chrome`, заголовок — `#gadget-<numberId>-title`, контент — `.dashboard-item-content`.

---

## 🧱 Структура модулей (ESM) и сборка

- `src/constants.js` — константы (селекторы, ключи, интервалы).
- `src/utils/dom.js` — `el`, `empty`, `autosizeTextarea`.
- `src/utils/poll.js` — `pollForGadget`.
- `src/api/jira.js` — `jiraRequest`.
- `src/storage/index.js` — `loadTasks`, `saveTasks`.
- `src/dnd/index.js` — `startDrag`, `persistOrderFromDom` (возвращает новый массив задач), внутренние функции для обработки mousedown/mousemove/mouseup, создания клона и индикатора вставки.
- `src/ui/header.js` — `updateHeaderCounts`.
- `src/ui/rerender.js` — `rerenderList(listEl, tasks, renderItem)`.
- `src/ui/item.js` — `renderItem`, `startEditMode`.
- `src/ui/index.js` — `renderUI` (форма, список, демо‑панель API).
- `src/bootstrap.js` — `setupGlobalMenuCloser`, `bootstrap`, `main`.
- `src/index.js` — entry‑point (вызов `main()`).

Сборка (esbuild):
- `scripts/header.txt` — баннер с метаданными Tampermonkey (должен быть первым в итоговом файле).
- `scripts/build.mjs` — сборка в формат IIFE с баннером, sourcemap inline.
- Скрипты npm:
  - `npm run build` — собрать в `dist/jira-mini-tasks.user.js`
  - `npm run dev` — режим наблюдения
