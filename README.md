# Jira Mini Tasks

Персональный список задач с интеграцией в Jira Dashboard.

## 🎯 Возможности плагина

### ✏️ Создание и редактирование задач
- **Добавление задач**: Введите текст и нажмите `Enter` или кнопку `➤`
- **Редактирование**: Кликните на задачу для редактирования
- **Сохранение изменений**: `Enter` для сохранения, `Esc` для отмены
- **Удаление**: Нажмите крестик в правом верхнем углу задачи
- **Переносы строк**: Все переносы строк сохраняются в тексте задачи

### 🔗 Интеграция с Jira
- **Автоматическое распознавание**: Введите код задачи (например, `UI-5788`)
- **Получение данных**: Автоматически загружается название задачи из Jira
- **Кликабельная ссылка**: Задача отображается как кнопка с логотипом Jira и усеченным названием (25 символов)
- **Удаление связи**: Наведите курсор на кнопку Jira и нажмите крестик
- **Валидация**: Если задача не найдена, появится красная рамка и сохранение будет заблокировано

### 📅 Временные метки
Добавьте дату через символ `@`:

**Поддерживаемые ключевые слова:**
- `@today` / `@сегодня` — Сегодня
- `@tomorrow` / `@завтра` — Завтра
- `@thisweek` / `@этанеделя` — Эта неделя
- `@nextweek` / `@следнеделя` — Следующая неделя
- `@later` / `@позже` / `@позднее` — Позже
- `@забыто` — Забытая задача (год назад)

**Отображение:**
- Дата показывается как кнопка с символом `＠`
- При наведении появляется точная дата или диапазон дат
- Просроченные задачи отмечаются красным цветом
- Удаление даты через крестик при наведении

### ✅ Управление статусом
- **Отметка выполнения**: Кликните чекбокс слева от задачи
- **Визуальное оформление**: Выполненные задачи получают серый фон и зеленую метку "✓ Выполнено"
- **Автоматическая сортировка**: Выполненные задачи всегда внизу списка
- **Удаление даты**: При отметке выполнения временная метка автоматически удаляется

### 🔄 Сортировка
- **Кнопка "⇅ По дате"**: Сортирует задачи по временным меткам
- **Порядок**: Сегодня → Завтра → Эта неделя → Следующая неделя → Позже → Забыто → Без даты
- **Состояния кнопки**:
  - Серо-голубой (неактивна)
  - Голубой (активна после нажатия)
  - Возврат к серо-голубому после перетаскивания

### 🎨 Drag & Drop
- **Перетаскивание**: Зажмите и перетащите задачу для изменения порядка
- **Автосброс сортировки**: После перетаскивания кнопка сортировки деактивируется
- **Сохранение порядка**: Измененный порядок сохраняется автоматически

### 📋 Интеграция на странице Jira
- **Автоматическое отображение**: При открытии задачи Jira, если она есть в вашем списке, под заголовком появляется блок с информацией
- **Показывается**:
  - Временная метка (если есть)
  - Текст задачи
  - Время создания задачи
- **Дизайн**: Синий фон, максимальная ширина 900px, отступ слева 20px

---

## 🚀 Установка

1. Установите расширение [Tampermonkey](https://www.tampermonkey.net/) для вашего браузера
2. Скачайте файл `dist/jira-mini-tasks.user.js`
3. Откройте Tampermonkey Dashboard и импортируйте файл
4. Готово! Откройте [Jira Dashboard](https://jira.theteamsoft.com/secure/)

## 🛠️ Разработка

### Сборка проекта
```bash
npm install              # Установить зависимости
npm run build           # Собрать проект → dist/jira-mini-tasks.user.js
npm run dev             # Режим разработки с автосборкой
```

### Структура проекта
```
src/
├── constants.js        # Константы (селекторы, ключи, интервалы)
├── utils/              # Утилиты (DOM, polling)
├── api/jira.js         # Обертка над Jira REST API
├── storage/index.js    # Работа с localStorage
├── dnd/index.js        # Drag & Drop логика
├── ui/                 # Компоненты интерфейса
├── bootstrap.js        # Инициализация виджета
├── index.js            # Точка входа (роутинг)
└── jira-page-integration.js  # Интеграция на странице Jira

scripts/
├── header.txt          # Метаданные для Tampermonkey
└── build.mjs           # Конфигурация сборки (esbuild)
```
